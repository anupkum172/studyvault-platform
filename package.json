{
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend -- --host 127.0.0.1 --port 5273 --strictPort\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
